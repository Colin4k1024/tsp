# TypeScript Frontend

## React / Next 基线

- 组件边界明确，页面、区块、基础组件分层。
- 服务端数据和本地 UI 状态不要无序混放。
- loading、empty、error、success 状态完整可见。

## 前端治理

- 样式优先走 design token，不散落硬编码视觉值。
- 交互必须可访问，键盘、焦点、标签和反馈路径完整。
- 涉及前端质量时，同时遵循平台的 `frontend-*` 规则。
