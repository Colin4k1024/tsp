# Common Hooks

## 当前策略

- 本仓库的 hooks skeleton 以“先有结构、后启用”为原则。
- `hooks/hooks.json` 默认禁用，不要求 Node 运行时。
- Python 占位脚本只提供接口契约和启用说明，不默认执行副作用。

## 启用门槛

- 明确触发事件、输入输出、失败回退与禁用策略。
- 对每个 hook 说明它是“提示类”“持久化类”还是“阻断类”。
- 不把必须成功的核心流程绑定到尚未验证的 hook 上。
